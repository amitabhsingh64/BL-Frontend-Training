let fruits = ["apple", "banana", "orange", "watermelon"];
console.log(fruits);
// fruits.push("coconut");  append at the end
// fruits.pop() remove last element
// fruits.unshift(); add Element to the beginning
// fruits.shift(); removes Element from the beginning
//let fruit_length = fruits.length;
//fruits.indexOf("apple"); first occurence idx, -1 if not
for(let i=0;i<fruits.length;i++){
    console.log(fruits[i]);
}